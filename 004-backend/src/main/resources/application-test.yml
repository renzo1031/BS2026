spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campus_buddy_test?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf8&serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root
  sql:
    init:
      mode: always
      encoding: UTF-8
      schema-locations: file:../sql/004/schema.sql
      data-locations: file:../sql/004/data.sql

campus-buddy:
  jwt:
    secret: test-only-jwt-secret-that-is-at-least-thirty-two-bytes
  identity:
    hmac-secret: test-only-identity-hmac-secret-32-bytes
    encryption-secret: test-only-encryption-secret-32-bytes
  wechat:
    dev-login-enabled: true
